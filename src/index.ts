const small = 1;
const medium = 2;
const large = 3;

// PascalCase, enum represents list of constants
const enum Size { Small = 1, Medium, Large }
let mySize: Size = Size.Medium;
console.log(mySize);

